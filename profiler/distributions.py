# Implemented in Milestone 3
import pandas as pd


def analyze_distributions(df: pd.DataFrame) -> dict:
    """Return normality p-values and skew labels per numeric column."""
    raise NotImplementedError
