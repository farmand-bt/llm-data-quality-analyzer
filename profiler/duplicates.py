# Implemented in Milestone 4
import pandas as pd


def analyze_duplicates(df: pd.DataFrame) -> dict:
    """Return exact duplicate count, percentage, and sample indices."""
    raise NotImplementedError
