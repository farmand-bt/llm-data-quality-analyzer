# Implemented in Milestone 4
def generate_recommendations(report: dict) -> list[dict]:
    """Return a list of rule-based recommendations derived from the report dict.

    Each recommendation has: severity (info|warning|critical), category, column, message.
    """
    raise NotImplementedError
