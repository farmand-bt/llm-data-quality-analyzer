# Implemented in Milestone 2
import pandas as pd


def detect_types(df: pd.DataFrame) -> dict:
    """Return inferred type info per column. Types: numeric, categorical, datetime, boolean, text, mixed."""
    raise NotImplementedError
