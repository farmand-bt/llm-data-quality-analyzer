# Implemented in Milestone 5
import pandas as pd


def render(df: pd.DataFrame, report: dict) -> pd.DataFrame:
    """Render interactive cleaning controls; return the cleaned DataFrame."""
    raise NotImplementedError
